# GoShopping 本日用実機テストチェックリスト

**テスト日**: 2026-03-16
**テスト実行者**: ユーザー（SH-54D / Pixel 9 / Windows）
**対象ビルド**: `flutter build apk --debug --flavor prod`
**主目的**: 2026-03-15 修正のうち、本日作業所で確認可能な Android / Windows 側の回帰を優先検証する。iOS ホワイトボード描画確認は自宅 Mac 環境での持ち越しとする。

---

## 1. 事前準備

- [ ] Android 端末 A / B を用意する
- [ ] Windows 端末を用意する
- [ ] テスト用アカウントを決める（ユーザー A / ユーザー B / 共有グループ用）
- [ ] 既存の共有グループとホワイトボード確認用データを 1 件以上用意する
- [ ] 最新ビルドをインストールしてログ監視を開始する

### 本日の制約

- [x] 現在は作業所で作業しており、Mac は自宅にある
- [x] iOS 端末 / iOS Simulator テストは本日実施不可
- [x] iOS ホワイトボード検証は自宅 Mac 環境で後日実施する

### 本日端末構成

- 端末 A: SH-54D すもも
- 端末 B: Pixel 9 まや
- 端末 C: Windows しんや
- 端末 D: iPhone 16e Simulator（ID: 89C2977C-F407-4F73-914C-BFC95398E11B, 本日は未使用）

### Android 実機用コマンド

```bash
flutter build apk --debug --flavor prod
adb -s 359705470227530 install -r build\app\outputs\flutter-apk\app-prod-debug.apk
adb -s 51040DLAQ001K0 install -r build\app\outputs\flutter-apk\app-prod-debug.apk
adb devices
```

### iOS Simulator 用コマンド（自宅 Mac 環境で実施）

```bash
flutter run -d 89C2977C-F407-4F73-914C-BFC95398E11B
```

### ログ監視例

```bash
adb logcat | Select-String "flutter|WHITEBOARD|DRAWING|SIGNIN|APP_INIT|HYBRID|HIVE|SYNC|GROUP|LIST"
```

---

## 2. 今日の重点確認項目

- [ ] サインアウト後に別ユーザーで再サインインしても、0 件誤判定や `SharedGroup box is not open` が再発しない
- [x] 別ユーザー再サインイン後、新ユーザーのグループのみ表示される
- [ ] Android / Windows 間でホワイトボード保存・復元・他端末反映が壊れていない

### 本日未実施項目（環境制約により保留）

- [ ] iOS でホワイトボードの線が点状に分断されず、連続した 1 本の線として描画・保存される
- [ ] iOS で複数回のストロークを描いても意図せず細かく分裂しない

### 本日の実施順

1. Android でサインアウト → 別ユーザー再サインイン確認
2. Android / Windows でホワイトボード保存・同期回帰確認
3. 時間が残れば cold start / 再起動 / Windows 側の回帰確認
4. iOS ホワイトボード描画確認は自宅 Mac 環境へ持ち越し

---

## 3. 持ち越し確認: iOS ホワイトボード描画連続性

**実施条件**: 自宅 Mac 環境に戻ってから実施

### 3.1 単一ストローク連続描画

- [ ] iOS で共有ホワイトボードを開く
- [ ] 黒色・標準線幅でゆっくり 1 本線を描く
- [ ] 描画直後に点線状ではなく連続線として見える
- [ ] 保存後に再読込しても同じ 1 本線として見える
- [ ] Android 端末で見ても同じ形で表示される

結果:

備考:

### 3.2 高速描画・長距離描画

- [ ] iOS で素早く長い線を 3 本以上描く
- [ ] 画面の対角方向に大きく線を引く
- [ ] 高速描画でも細かい点や短線に勝手に分断されない
- [ ] 保存後の再表示でも分断が増えない
- [ ] Android 側で見ても大きな欠損がない

結果:

備考:

### 3.3 複数ストローク境界の正常性

- [ ] 1 本目を描いてペンを離す
- [ ] 2 本目を別位置に描く
- [ ] 3 本目を別色で描く
- [ ] それぞれが独立したストロークとして保持される
- [ ] 1 本の線が不自然に複数分割されず、逆に別々の線が結合もしない

結果:

備考:

### 3.4 Undo / Redo / 再保存回帰

- [ ] 描画後に Undo が動作する
- [ ] Redo で元に戻る
- [ ] Undo / Redo 後に保存できる
- [ ] 保存後に再度開いても状態が維持される
- [ ] 他端末表示でも破綻しない

結果:

備考:

---

## 4. 最優先確認: 別ユーザー再サインイン時の復元安定化

### 4.1 サインアウト → 別ユーザー再サインイン

- [x] ユーザー A でサインインして所属グループを確認する
- [x] サインアウトする
- [x] ユーザー B でサインインする
- [x] サインイン直後にクラッシュしない
- [ ] `SharedGroup box is not open` エラーがログに出ない
- [x] ユーザー B のグループが表示される
- [x] ユーザー A 専用グループが残らない

結果:

部分確認 OK

備考:

- Pixel 9 まやでサインイン → サインアウト → すももで再サインインを実施
- グループ表示に問題なし。すもも側の所属グループのみ表示され、まや側のグループ残留は確認されなかった
- `SharedGroup box is not open` のログ確認は未実施のため、この項目は保留

### 4.2 0 件誤判定の有無

- [ ] グループを持つユーザーでサインインする
- [ ] 一瞬 0 件に見えても、そのまま固定されない
- [ ] Firestore 復元後に正しいグループ数で安定する
- [ ] 不要な「最初のグループを作成」導線へ誤遷移しない
- [ ] 新規グループ作成を契機に旧グループが後からまとめて出てこない

結果:

備考:

### 4.3 A → B → A の往復切替

- [x] ユーザー A からユーザー B へ切り替える
- [x] ユーザー B から再度ユーザー A に戻す
- [x] それぞれの所属グループだけが表示される
- [x] 相手ユーザーのグループが混在しない
- [x] リストや選択状態も不自然に引き継がれない

結果:

確認 OK

備考:

- Pixel 9 で すもも → まや → すもも の往復切替を実施
- グループ一覧の大きな崩れは確認されず、所属グループ表示も問題なし
- 自分が参加していない他ユーザーのグループが表示されないことも確認済み
- 修正後の再確認で、グループ詳細画面の WB プレビューパネルも表示された
- リスト選択状態がユーザー切り替えで引き継がれていないことも確認済み

---

## 5. 回帰確認: ホワイトボード保存・同期

### 5.1 保存と他端末反映

- [x] 端末 A または B で描画して保存する
- [x] 別の Android 端末または Windows 端末で反映される
- [x] 反映先端末から再度開いても内容が維持される
- [x] プレビューにも反映される
- [ ] 再保存後も重複ストロークが増えない

結果:

部分確認 OK

備考:

- Pixel 9 のユーザー切替後に発生していた WB プレビュー欠落は、修正後の再確認で解消
- 三者共有グループのホワイトボードで描画内容が他端末へ即反映されることを確認
- プレビュー、編集画面ともに正常表示を確認
- いったん他グループへ移動して同様の確認後、元グループへ戻っても正常表示を維持
- USB接続の SH-54D でホワイトボード保存ボタン押下後、保存スピナーが正常に消えることを確認
- 重複ストローク有無は引き続き未確認

### 5.2 個人ホワイトボード / 共有ホワイトボード

- [x] 共有ホワイトボードを開ける
- [x] 個人ホワイトボードを開ける
- [ ] 閲覧専用時に描画できない
- [x] 保存できる対象では通常どおり保存できる
- [x] Android / Windows 範囲で今回の回帰が再発しない

結果:

部分確認 OK

備考:

- Pixel 9 でサインイン切替後に出ていたグループ詳細画面のホワイトボードプレビュー欠落は再現せず
- 共有ホワイトボードはプレビュー、編集画面ともに正常
- 他グループへ移動して確認後、再度戻っても正常に表示された
- 個人用ホワイトボードでも保存スピナーは残らず、保存後のUI反応低下も確認されなかった
- 閲覧専用時の挙動は未確認

---

## 6. 回帰確認: 再起動 / cold start / Windows

### 6.1 Android cold start

- [ ] 認証済みのままアプリを完全終了する
- [ ] アプリを再起動する
- [ ] 既存グループが安定して表示される
- [ ] 一瞬 0 件になってもその後の最終表示が正しい
- [ ] 手動同期なしで表示が落ち着く

結果:

備考:

### 6.2 Windows 側のユーザー切替回帰

- [ ] Windows でユーザー A のグループ表示を確認する
- [ ] サインアウトしてユーザー B でサインインする
- [ ] Hive 初期化直後の空状態を正として固定しない
- [ ] B のグループのみ表示される
- [ ] 新規グループ作成を契機に旧グループが再出現しない

結果:

備考:

---

## 7. NG 判定条件

- [ ] iOS で 1 本の線が点状・短線状に分断される
- [ ] 保存後または再読込後にストローク分断が悪化する
- [ ] サインイン切替時に `SharedGroup box is not open` が再発する
- [ ] 別ユーザー再サインイン後にグループ 0 件のまま固まる
- [ ] 別ユーザーのグループやリストが混在する
- [ ] ホワイトボード保存後に他端末表示やプレビューが壊れる

---

## 8. 実施結果

### 端末構成

- 端末 A: SH-54D すもも（359705470227530）
- 端末 B: Pixel 9 まや（51040DLAQ001K0）
- 端末 C: Windows しんや
- 端末 D: iPhone 16e Simulator（89C2977C-F407-4F73-914C-BFC95398E11B, 保留）

### 判定

- [ ] PASS
- [ ] FAIL

### 備考

```text
未解決事項・追加で見つかった問題をここに記載

- iOS ホワイトボード描画の連続性確認は、自宅 Mac 環境で別途実施する
- オレンジの接続エラーバナーが出ることがある
- ただし同期自体には問題なく、AppBar の同期状態アイコンも正常表示
- 手動同期を実行するとバナーは解消される
- ネットワーク監視バナー側の判定が厳しすぎる可能性あり
```
