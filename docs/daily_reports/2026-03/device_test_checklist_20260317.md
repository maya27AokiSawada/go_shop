# GoShopping 本日用実機テストチェックリスト

**テスト日**: 2026-03-17
**テスト実行者**: ユーザー（Pixel 9 / SH-54D / Windows）
**対象ビルド**: `flutter build apk --debug --flavor prod`
**主目的**: 2026-03-16 時点の未実施項目を回収しつつ、ホワイトボード保存復帰、個人用ホワイトボード即時反映、ネットワーク監視バナー誤検知緩和の回帰を、Pixel 9・SH-54D・Windows 実機で重点確認する。iOS 確認はマシン未確保のため後回しとする。

---

## 1. 事前準備

- [ ] Android 端末 A / B を用意する（Pixel 9 / SH-54D）
- [ ] Windows 端末を用意する
- [ ] テスト用アカウントを決める（オーナー / メンバー / 別ユーザー）
- [ ] 共有ホワイトボード、個人用ホワイトボード、共有グループを 1 件以上用意する
- [ ] 最新ビルドをインストールしてログ監視を開始する

### 本日の実施条件

- [x] 本日の実施端末は Pixel 9 / SH-54D / Windows とする
- [x] iOS は確認用マシン未確保のため本日は実施しない
- [x] iOS 項目は後日回収対象として残す

### 想定端末構成

- 端末 A: SH-54D すもも
- 端末 B: Pixel 9 まや
- 端末 C: Windows しんや

### Android 実機用コマンド

```bash
flutter build apk --debug --flavor prod
adb -s 359705470227530 install -r build\app\outputs\flutter-apk\app-prod-debug.apk
adb -s 51040DLAQ001K0 install -r build\app\outputs\flutter-apk\app-prod-debug.apk
adb devices
```

### Windows 用コマンド

```bash
flutter build windows
flutter run -d windows
```

### ログ監視例

```bash
adb logcat | Select-String "flutter|WHITEBOARD|DRAWING|SIGNIN|APP_INIT|HYBRID|HIVE|SYNC|NETWORK|NOTIFICATION"
```

---

## 2. 今日の重点確認項目

### 本日確認済み項目

- [x] サインイン / サインアウト処理確認
- [x] グループ、リスト、アイテムの CRUD 確認
- [x] 個人用ホワイトボードの編集不可スイッチ ON → OFF を確認

- [ ] 共有ホワイトボード / 個人用ホワイトボードとも保存後にスピナーが残らない
- [ ] 閲覧専用状態では描画できない
- [ ] 再保存や他端末再読込で重複ストロークが増えない
- [ ] 個人用ホワイトボードの編集可 / 編集不可切り替えが一覧と editor に即時反映される
- [ ] 一時的な通信失敗だけではオフラインバナーが出続けない
- [ ] サインイン切替直後に `SharedGroup box is not open` や 0 件誤判定が出ない

### 本日保留する項目

- [ ] iOS でホワイトボードの線が点状に分断されず、連続した 1 本線として描画・保存される

### 本日の実施順

1. Pixel 9 / SH-54D / Windows でホワイトボード保存・同期・閲覧専用制御を確認する
2. ネットワーク監視バナーの誤検知緩和を確認する
3. 最後にユーザー切替と cold start の回帰を軽く確認する
4. iOS 項目は後日回収する

---

## 3. 後日回収: iOS ホワイトボード描画連続性

**実施方針**: iOS は本日テスト用マシンがないため未実施。項目は削除せず、そのまま次回回収用として保持する。

### 3.1 単一ストローク連続描画

- [ ] iOS で共有ホワイトボードを開く
- [ ] 黒色・標準線幅でゆっくり 1 本線を描く
- [ ] 描画直後に点線状ではなく連続線として見える
- [ ] 保存後に再読込しても同じ 1 本線として見える
- [ ] Android 端末で見ても同じ形で表示される

結果:

備考:

### 3.2 高速描画・長距離描画

- [ ] iOS で素早く長い線を 3 本以上描く
- [ ] 画面の対角方向に大きく線を引く
- [ ] 高速描画でも細かい点や短線に勝手に分断されない
- [ ] 保存後の再表示でも分断が増えない
- [ ] Android / Windows 側で見ても大きな欠損がない

結果:

備考:

### 3.3 複数ストローク境界と Undo / Redo

- [ ] 1 本目を描いてペンを離す
- [ ] 2 本目を別位置に描く
- [ ] 3 本目を別色で描く
- [ ] それぞれが独立したストロークとして保持される
- [ ] Undo / Redo 後に保存しても破綻しない

結果:

備考:

---

## 4. 最優先確認: ホワイトボード保存・同期・権限制御

### 4.1 保存スピナー復帰

- [ ] 共有ホワイトボードを開いて描画する
- [ ] 保存後にスピナーが正常に消える
- [ ] 個人用ホワイトボードでも同様に保存する
- [ ] 個人用ホワイトボードでもスピナーが残らない
- [ ] 保存後に UI 反応が低下しない

結果:

概ね OK

備考:

- ホワイトボード保存後にスピナーが消える挙動は全体として改善を確認
- ただし「他人の個人用ホワイトボード」を編集した場合のみ、まだ問題が残っている
- ルール起因かどうかは切り分け継続。現時点では Firestore Security Rules よりも、アプリ側の保存経路または例外ハンドリングの可能性を疑う

### 4.2 他端末反映と重複ストローク

- [ ] 端末 A で共有ホワイトボードを描画して保存する
- [ ] 端末 B または Windows で即時反映を確認する
- [ ] 反映先から再度開いて内容が維持される
- [ ] 同じホワイトボードを再保存しても重複ストロークが増えない
- [ ] プレビュー表示でも内容が一致する

結果:

備考:

### 4.3 閲覧専用時の描画禁止

- [ ] 編集不可の個人用ホワイトボードを表示する
- [ ] editor を開く
- [ ] ペン入力を試しても新規描画できない
- [ ] 保存ボタンや編集導線が不正に有効化されない
- [ ] 一覧側の表示と editor 側の状態が一致する

結果:

備考:

### 4.4 個人用ホワイトボード公開設定の即時反映

- [ ] 端末 A で個人用ホワイトボードの編集可 / 編集不可を切り替える
- [ ] 一覧側の表示が即時更新される
- [ ] editor を開き直さなくても状態が一致する
- [ ] 端末 B または Windows 側にも即時反映される
- [ ] 逆方向の切り替えでも同じ動作になる

結果:

部分確認 OK

備考:

- 編集不可スイッチの ON → OFF は確認済み
- UI 表示と切り替え自体は概ね期待どおり
- 他人の個人用ホワイトボード保存まわりは別論点として継続確認

---

## 5. 重点確認: ネットワーク監視バナー誤検知緩和

### 5.1 単発失敗で offline 扱いにならないこと

- [ ] 端末 A を通常オンライン状態で使用する
- [ ] 軽い通信揺らぎや一時的な切断状態を作る
- [ ] Firestore 操作が成功している間はオフラインバナーが出続けない
- [ ] 単発失敗後に自動復帰して online 表示へ戻る
- [ ] `Failed to resolve name` 相当の一時失敗だけで恒久 offline にならない

結果:

備考:

### 5.2 連続失敗時だけ offline になること

- [ ] 端末 A を明確にオフラインにする
- [ ] 連続で接続チェックが失敗する状況を作る
- [ ] 一定回数失敗後に offline バナーが表示される
- [ ] オンライン復帰後はバナーが解消される
- [ ] Firestore 成功通知後に状態が stuck しない

結果:

備考:

---

## 6. 回帰確認: サインイン切替 / cold start

### 6.1 別ユーザー再サインイン

- [ ] ユーザー A でサインインして所属グループを確認する
- [ ] サインアウトする
- [ ] ユーザー B でサインインする
- [ ] サインイン直後にクラッシュしない
- [ ] `SharedGroup box is not open` がログに出ない
- [ ] ユーザー B のグループのみ表示される
- [ ] ユーザー A のグループが残らない

結果:

備考:

### 6.2 0 件誤判定の有無

- [ ] グループを持つユーザーでサインインする
- [ ] 一瞬 0 件に見えてもそのまま固定されない
- [ ] Firestore 復元後に正しいグループ数で安定する
- [ ] 不要な「最初のグループを作成」導線へ誤遷移しない
- [ ] 新規グループ作成を契機に旧グループが後から再出現しない

結果:

備考:

### 6.3 cold start / 再起動

- [ ] 認証済み状態のままアプリを完全終了する
- [ ] アプリを再起動する
- [ ] 既存グループが安定して表示される
- [ ] 手動同期なしで最終表示が正しい
- [ ] ホワイトボードプレビューも正常表示される

結果:

備考:

---

## 7. NG 判定条件

- [ ] iOS で 1 本の線が点状・短線状に分断される
- [ ] 保存後または再読込後にストローク分断が悪化する
- [ ] 保存後に共有 / 個人用ホワイトボードのスピナーが残る
- [ ] 閲覧専用なのに描画や保存ができる
- [ ] 同じ描画が再保存や再読込で重複する
- [ ] `isPrivate` 切り替えが一覧と editor で食い違う
- [ ] 単発通信失敗だけで offline バナーが出続ける
- [ ] サインイン切替直後に `SharedGroup box is not open` や 0 件固定が発生する

---

## 8. 実施結果

### 端末構成

端末A:
端末B:
端末C:
端末D: 未使用（iOS 後日）

### 判定

- [ ] PASS
- [ ] FAIL

### 備考

```text
本日確認できたこと:
- サインイン / サインアウト処理は確認OK
- グループ、リスト、アイテムのCRUDは確認OK
- 個人用ホワイトボードの編集不可スイッチ ON→OFF は確認OK

継続確認事項:
- ホワイトボード保存でスピナーが消える挙動は概ねOK
- ただし他人の個人用ホワイトボードのみ問題が残っている
- Firestore Security Rules の whiteboards サブコレクションは、現状でも親グループメンバーの read / create / update を許可しているため、ルールだけが原因とは言い切れない
```
