# GoShopping 実機総合テストチェックリスト

**テスト日**: 2026-03-25
**テスト実行者**: ユーザー（Pixel 9 / SH-54D / Windows）
**対象ビルド**: `flutter build apk --debug --flavor prod`
**主目的**: 2026-03-17 以降の修正（ホワイトボード保存 arrayUnion 化、通知履歴スピナー修正、ErrorLogService カバレッジ拡大、アイテム編集権限制御、個人用ボードオーナー専用化）を実機で検証し、全体的な回帰テストを行う。

---

## 1. 事前準備

- [ ] Android 端末 A / B を用意する（Pixel 9 / SH-54D）
- [ ] Windows 端末を用意する
- [ ] テスト用アカウントを決める（オーナー / メンバー / 別ユーザー）
- [ ] 共有ホワイトボード、個人用ホワイトボード、共有グループを 1 件以上用意する
- [ ] 最新ビルドをインストールしてログ監視を開始する

### 想定端末構成

- 端末 A: SH-54D すもも
- 端末 B: Pixel 9 まや
- 端末 C: Windows しんや

### Android 実機用コマンド

```bash
flutter build apk --debug --flavor prod
adb -s 359705470227530 install -r build\app\outputs\flutter-apk\app-prod-debug.apk
adb -s 51040DLAQ001K0 install -r build\app\outputs\flutter-apk\app-prod-debug.apk
adb devices
```

### Windows 用コマンド

```bash
flutter build windows
flutter run -d windows
```

### ログ監視例

```bash
adb logcat | Select-String "flutter|WHITEBOARD|DRAWING|SIGNIN|APP_INIT|HYBRID|HIVE|SYNC|NETWORK|NOTIFICATION|ERROR_LOG"
```

---

## 2. 今回の重点確認項目（2026-03-17 以降の修正）

### 2.1 ホワイトボード保存 arrayUnion 化 (`85a0121`)

- [ ] 共有ホワイトボードで描画して保存 → スピナーが 2 秒以内に消える
- [ ] 個人用ホワイトボードで描画して保存 → スピナーが 2 秒以内に消える
- [ ] 保存後に再読込しても重複ストロークが増えない
- [ ] 端末 A で保存 → 端末 B で即時反映される
- [ ] 大量ストローク（10 本以上）でも保存がハングしない

結果:

備考:

### 2.2 通知履歴スピナー固着修正 (`03874b6`)

- [ ] 通知履歴画面を開く → スピナーが消えて一覧が表示される
- [ ] 通知がない場合は「通知はありません」と表示される
- [ ] 画面を閉じて再度開いても正常に表示される
- [ ] 通知履歴画面を素早く開閉しても固着しない

結果:

備考:

### 2.3 通知リスナー死亡バグ修正 (`03874b6`)

- [ ] アプリを長時間（5分以上）バックグラウンドに置く
- [ ] フォアグラウンドに戻して通知が引き続き受信される
- [ ] 通信が一時切断後に復帰しても通知リスナーが生きている
- [ ] ログに `_isListening` リセット関連のエラーが出ない

結果:

備考:

### 2.4 アイテム編集ダイアログ権限制御 (`99db590`)

- [ ] オーナーがアイテムをダブルタップ → 編集ダイアログが開く
- [ ] 権限のないメンバーがアイテムをダブルタップ → 編集ダイアログが開かない
- [ ] 権限のないメンバーでも購入チェック（タップ）は可能
- [ ] 購入状態の切り替えは正常に同期される

結果:

備考:

### 2.5 ErrorLogService カバレッジ確認 (`f9e2f97`, `1eb2c87`)

- [ ] ホワイトボード保存失敗時にエラー履歴に記録される
- [ ] サインイン失敗時にエラー履歴に記録される
- [ ] 購入状態更新失敗時にエラー履歴に記録される
- [ ] グループ名更新失敗時に「グループ名の更新に失敗しました」と表示される（旧: 「削除に失敗しました」が修正済み）
- [ ] 設定画面からエラー履歴を確認できる

結果:

備考:

### 2.6 個人用ボードオーナー専用編集 (`65d759f`)

- [ ] 個人用ホワイトボードはオーナーのみ編集可能
- [ ] 他メンバーの個人用ホワイトボードは閲覧のみ
- [ ] 閲覧専用時にツールバーが非表示
- [ ] 閲覧専用時に「閲覧専用」バーが表示される

結果:

備考:

---

## 3. ホワイトボード機能テスト

### 3.1 描画・保存・同期

- [ ] 6 色カラーピッカーが表示され選択した色で描画できる
- [ ] ペン太さ 5 段階で選択できる
- [ ] 複数ストロークの描画と自動分割が正常に動作する
- [ ] 保存ボタンで保存 → 完了メッセージが表示される
- [ ] 再度開いた時に描画内容が復元される
- [ ] 端末 A → 端末 B への同期が 3 秒以内

結果:

備考:

### 3.2 ツールバー・モード切替

- [ ] 上段: カラーボタン + スクロール/描画モード切替
- [ ] 下段: ペン太さ + ズーム ± + 全消去
- [ ] 描画モード中: 描画可能、スクロール不可
- [ ] スクロールモード中: スクロール可能、描画不可
- [ ] モード切替時にスクロール位置が不自然にリセットされない (`2d56d20`)

結果:

備考:

### 3.3 ズーム機能

- [ ] ズームアウト（-）/ ズームイン（+）が動作する
- [ ] 現在のズーム倍率が表示される
- [ ] 拡大時にスクロール範囲が自動調整される

結果:

備考:

### 3.4 全消去機能

- [ ] 全消去ボタン → 確認ダイアログ表示
- [ ] 確認後にすべての描画が消去される
- [ ] 全消去失敗時にエラー履歴に記録される

結果:

備考:

### 3.5 編集ロック (`bf5a2dd`)

- [ ] 端末 A が編集中に端末 B が開くと編集ロック状態が見える
- [ ] ロックタイムアウト後は端末 B も編集可能になる
- [ ] stale ロック引き継ぎが正常に動作する

結果:

備考:

---

## 4. 後日回収: iOS ホワイトボード描画連続性

**実施方針**: iOS はテスト用マシン未確保のため後日実施。項目は削除せず保持する。

### 4.1 単一ストローク連続描画

- [ ] iOS で共有ホワイトボードを開く
- [ ] 黒色・標準線幅でゆっくり 1 本線を描く → 連続線として見える
- [ ] 保存後に再読込しても同じ 1 本線として見える
- [ ] Android 端末で見ても同じ形で表示される

### 4.2 高速描画・長距離描画

- [ ] iOS で素早く長い線を 3 本以上描く
- [ ] 高速描画でも細かい点や短線に分断されない
- [ ] 保存後の再表示でも分断が増えない

---

## 5. 基本機能テスト

### 5.1 サインイン / サインアウト

- [ ] サインインが正常に完了する
- [ ] サインアウトが正常に完了する
- [ ] サインイン失敗時にエラーメッセージとエラー履歴記録がある
- [ ] パスワードリセットが動作する

結果:

備考:

### 5.2 グループ管理

- [ ] 新しいグループを作成できる
- [ ] グループ名を変更できる
- [ ] QR コードで招待できる
- [ ] QR コードをスキャンして参加できる
- [ ] メンバーリストが正しく表示される
- [ ] グループ詳細画面: ランドスケープで左カラムが上揃えになっている

結果:

備考:

### 5.3 買い物リスト

- [ ] 新しいリストを作成できる
- [ ] アイテムを追加できる
- [ ] 購入状態を切り替えられる
- [ ] アイテムを削除できる
- [ ] リストを削除できる

結果:

備考:

---

## 6. 通知システムテスト

### 6.1 通知受信

- [ ] ホワイトボード保存時に他メンバーへ通知される
- [ ] グループ参加時に招待元へ通知される
- [ ] 通知内容が正確（更新者名、グループ名等）

結果:

備考:

### 6.2 通知履歴

- [ ] 通知履歴画面で過去の通知が一覧表示される
- [ ] スピナーが固着せず正常にローディングが完了する
- [ ] 通知がない場合は空表示になる

結果:

備考:

---

## 7. データ同期テスト

### 7.1 グループ同期

- [ ] 新規グループが他デバイスで 3 秒以内に表示される
- [ ] メンバー情報が正確に同期される

### 7.2 リスト同期

- [ ] アイテム追加が他デバイスで 3 秒以内に表示される
- [ ] 購入状態変更が他デバイスで 3 秒以内に反映される

### 7.3 オフライン動作

- [ ] オフライン時もアプリが正常動作する
- [ ] オンライン復帰時に自動同期される

結果:

備考:

---

## 8. ネットワーク監視バナー

- [ ] 通常オンラインでオフラインバナーが誤表示されない
- [ ] 一時的な通信揺らぎでバナーが出続けない
- [ ] 明確にオフラインの場合はバナーが表示される
- [ ] オンライン復帰後はバナーが解消される

結果:

備考:

---

## 9. 回帰確認: サインイン切替 / cold start

### 9.1 別ユーザー再サインイン

- [ ] ユーザー A でサインイン → サインアウト → ユーザー B でサインイン
- [ ] サインイン直後にクラッシュしない
- [ ] `SharedGroup box is not open` がログに出ない
- [ ] ユーザー B のグループのみ表示される

結果:

備考:

### 9.2 0 件誤判定の有無

- [ ] グループを持つユーザーでサインインする
- [ ] 一瞬 0 件に見えてもすぐに正しいグループ数で安定する
- [ ] 不要な「最初のグループを作成」導線へ誤遷移しない

結果:

備考:

### 9.3 cold start / 再起動

- [ ] 認証済み状態のままアプリを完全終了 → 再起動
- [ ] 既存グループが安定して表示される
- [ ] ホワイトボードプレビューも正常表示される

結果:

備考:

---

## 10. UI/UX レスポンシブ対応

- [ ] グループページ: 横幅 1000px 以上で左右分割レイアウト
- [ ] グループページ: 横幅 1000px 未満で縦積みレイアウト
- [ ] グループ詳細ランドスケープ: 左カラム上揃え
- [ ] パネル間の仕切り線が表示される

結果:

備考:

---

## 11. NG 判定条件

- [ ] 保存後にスピナーが 5 秒以上残る
- [ ] 通知履歴画面でスピナーが固着する
- [ ] 閲覧専用なのに描画や保存ができる
- [ ] 同じ描画が再保存や再読込で重複する
- [ ] 権限のないメンバーが編集ダイアログを開ける
- [ ] 単発通信失敗だけでオフラインバナーが出続ける
- [ ] サインイン切替直後に `SharedGroup box is not open` や 0 件固定が発生する
- [ ] ErrorLogService に記録されるべきエラーが記録されない

---

## 12. 実施結果

### 端末構成

- 端末 A:
- 端末 B:
- 端末 C:

### 判定

- [ ] PASS
- [ ] FAIL

### 備考

```text
確認できたこと:


継続確認事項:


検出された問題:

```

---

## 13. 検出された問題

| No. | 問題内容 | 重要度          | 状態       |
| --- | -------- | --------------- | ---------- |
| 1   |          | High/Medium/Low | Open/Fixed |
| 2   |          | High/Medium/Low | Open/Fixed |
| 3   |          | High/Medium/Low | Open/Fixed |
