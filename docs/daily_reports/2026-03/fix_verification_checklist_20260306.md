# セクション3-4 改修確認テスト チェックリスト

**テスト日**: 2026-03-06
**テスト実行者**: \_\_\_\_\_\_\_\_
**対象**: セクション3-4で検出された6件のバグ修正（P0×1, P1×3, P2×2）

---

## 事前準備

```bash
# 1. APKビルド
flutter build apk --debug --flavor prod

# 2. テストデバイスにインストール
adb install -r build\app\outputs\flutter-apk\app-prod-debug.apk

# 3. ログ監視
adb logcat | Select-String "flutter|HYBRID_REPO|NOTIFICATION|GROUP"
```

---

## Fix 1: P0 — FAB RenderFlexオーバーフロー修正

**修正ファイル**: `lib/pages/shared_group_page.dart`
**修正内容**: FABのColumn に `mainAxisSize: MainAxisSize.min` を追加

### テスト項目

- [ ] グループタブでFAB（＋ボタン）が画面内に正しく表示される
- [ ] FABをタップするとQRスキャンボタン＋グループ作成ボタンが展開される
- [ ] 展開時にRenderFlexオーバーフロー（赤い帯）が発生しない
- [ ] 小さい画面（縦画面）でもレイアウトが崩れない
- [ ] FABの各ボタンが正常にタップできる

**結果**: \_\_ / 5 合格

---

## Fix 2: P1 #1 — コピー付き作成でメンバーがコピーされない

**修正ファイル**: `lib/widgets/group_creation_with_copy_dialog.dart`
**修正内容**: `_createGroup()` と `_addMembersToNewGroup()` 内の `ref.watch()` → `ref.read()` に変更

### テスト項目

- [ ] 既存グループを選択してコピー付きグループ作成ができる
- [ ] コピー元グループのメンバーが選択ダイアログに表示される
- [ ] メンバーを選択して作成すると、新グループにメンバーが正しくコピーされる
- [ ] 作成後に新グループのメンバー管理画面でコピーされたメンバーが確認できる
- [ ] Firestoreにもメンバーが正しく保存されている（Firebase Console確認）

**結果**: \_\_ / 5 合格

---

## Fix 3: P1 #2 — コピー付き作成で自分に通知が来ない

**修正ファイル**: `lib/widgets/group_creation_with_copy_dialog.dart`
**修正内容**: `_sendMemberNotifications()` の後に自分自身への通知送信を追加

### テスト項目

- [ ] コピー付きでメンバー選択してグループ作成する
- [ ] 作成者自身に「グループ作成」通知が届く（通知履歴で確認）
- [ ] 選択したメンバーにも通知が届く
- [ ] 通常のグループ作成（コピーなし）でも自分に通知が届く（既存動作の退行確認）
- [ ] 他デバイスで同じアカウントでサインインしている場合、グループが反映される

**結果**: \_\_ / 5 合格

---

## Fix 4: P1 #3 — メンバーのグループ退出機能が動作しない

**修正ファイル**: `lib/widgets/group_list_widget.dart`
**修正内容**: `_showGroupOptions()` のオーナーでない場合の早期returnを削除

### 前提条件

- デバイスAでオーナーアカウントにサインイン
- デバイスBでメンバーアカウントにサインイン
- 両方が同じグループに所属

### テスト項目（メンバー操作）

- [ ] メンバーがグループをロングタップするとオプションメニューが表示される
- [ ] 「グループを退出」ダイアログ（オレンジ色）が表示される
- [ ] 退出を実行すると、UIからグループが消去される
- [ ] Firestore上で `members` と `allowedUid` から退出者が除去されている
- [ ] 退出後、再招待でグループに復帰できる

### テスト項目（オーナー操作 — 退行確認）

- [ ] オーナーがグループをロングタップするとオプションメニューが表示される
- [ ] 「グループを削除」ダイアログ（赤色）が表示される
- [ ] 削除を実行するとグループが完全に削除される
- [ ] 削除後に全メンバーに削除通知が送信される

**結果**: \_\_ / 9 合格

---

## Fix 5: P2 #1 — 再インストール後に認証切れでネットワーク障害バナー表示

**修正ファイル**: `lib/services/network_monitor_service.dart`
**修正内容**: `checkFirestoreConnection()` で `permission-denied` / `unauthenticated` 時に公開コレクション(`furestorenews`)でフォールバック確認

### テスト項目

- [ ] 通常起動時にネットワーク障害バナーが表示されない
- [ ] 機内モードで起動するとネットワーク障害バナーが表示される
- [ ] 機内モード解除後にバナーが消える

### 再インストールシナリオ

- [ ] アプリをアンインストールする
- [ ] APKを再インストールする
- [ ] 起動時にネットワーク障害バナーが**表示されない**（認証前でもオンライン判定）
- [ ] サインインすると正常にFirestoreに接続される
- [ ] サインイン後もバナーが表示されない

**結果**: \_\_ / 8 合格

---

## Fix 6: P2 #2 — 招待残り回数がデバイスUIに非表示

**修正ファイル**: `lib/pages/group_invitation_page.dart`
**修正内容**: QRコード下に「招待可能人数: 5人」の青色チップUIを追加

### テスト項目

- [ ] グループメンバー管理画面から招待ページを開く
- [ ] QRコード生成ボタンをタップしてQRコードを表示する
- [ ] QRコード下に「👥 招待可能人数: 5人」の青色チップが表示される
- [ ] チップのスタイル（青背景、青ボーダー、アイコン付き）が正しい
- [ ] QRコードを再生成しても表示が維持される

**結果**: \_\_ / 5 合格

---

## 総合結果サマリー

| Fix #  | 優先度 | 内容                             | 合格数        | 判定 |
| ------ | ------ | -------------------------------- | ------------- | ---- |
| 1      | P0     | FAB RenderFlexオーバーフロー     | \_\_ / 5      |      |
| 2      | P1     | コピー付き作成メンバー未コピー   | \_\_ / 5      |      |
| 3      | P1     | コピー付き作成自分に通知なし     | \_\_ / 5      |      |
| 4      | P1     | メンバーグループ退出機能不動作   | \_\_ / 9      |      |
| 5      | P2     | 再インストール後ネットワーク障害 | \_\_ / 8      |      |
| 6      | P2     | 招待残り回数非表示               | \_\_ / 5      |      |
| **計** |        |                                  | **\_\_ / 37** |      |

### 備考欄

```
（テスト中に気づいた点や新しい問題があればここに記載）


```
