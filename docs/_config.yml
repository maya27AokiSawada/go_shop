title: GoShopping
description: 家族の買い物リストを共有するアプリ
lang: ja

# Liquid エラーを警告扱いにして ${{ }} などで build が止まらないようにする
liquid:
  error_mode: lax

# daily_reports は ${{ }} を含むため Liquid 処理を無効化して公開
defaults:
  - scope:
      path: "daily_reports"
    values:
      render_with_liquid: false
