title: GoShopping
description: 家族の買い物リストを共有するアプリ
lang: ja

# Liquid エラーを警告扱いにして ${{ }} などで build が止まらないようにする
liquid:
  error_mode: lax

# daily_reports は大量の ${{ }} を含むため Jekyll 処理対象から除外
# ※ specifications / knowledge_base / troubleshooting は公開対象
exclude:
  - daily_reports
