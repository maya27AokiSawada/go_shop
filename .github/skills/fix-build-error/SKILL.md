---

name: fix-build-error
description:Flutter/Dartコードのビルドエラー修正を行う。

## ルール

- 必ず dart analyze の診断（error/warning）に従って修正する
- 推測やリファクタリングは禁止（analyzerに出ていない変更はしない）
- 構文エラー（セミコロン、括弧、クォート、型）だけ修正する
- ロジックの変更は禁止
- 修正は最小限にする

## 出力形式

- 修正前後の差分（diff形式）
- 修正理由（どのエラーに対応したか）

## 入力

（ここにコードとエラーメッセージを貼る）
``
